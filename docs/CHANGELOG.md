# Changelog

All notable changes to this project will be documented in this file.

## [Unreleased]

### Added

- Initial Next.js + TypeScript project setup using the App Router.
- Baseline folder structure: `src/app`, `src/components`, `src/lib`, `src/styles`, `public`, `docs`.
- Project documentation scaffolding (`README.md`, `docs/CHANGELOG.md`, `docs/FEATURES/`).
- Supabase client setup: browser client (`src/lib/supabase/client.ts`) and server client (`src/lib/supabase/server.ts`) using `@supabase/ssr`.
- Middleware (`src/middleware.ts`) that refreshes the Supabase auth session on each request.
- `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_ANON_KEY` added to `.env.example`.
- Login page (`src/app/login/page.tsx`) with an email/password form; no signup, as this app is invite-only.
- Server Action (`src/app/login/actions.ts`) that signs in via Supabase and redirects to `/dashboard` on success.
- Middleware now redirects unauthenticated requests to `/login`, leaving `/login` itself public.
- Tailwind CSS added for form styling.
- Projects dashboard (`src/app/dashboard/page.tsx`) listing client, project, status, and creation date for each project, linking through to `/projects/[id]`.
- New Project form (`src/app/dashboard/new/page.tsx`) and Server Action (`src/app/dashboard/new/actions.ts`) that inserts a row into `projects`, setting `owner_id` from the authenticated user.
- Confirmed existing auth middleware already covers `/dashboard` and `/dashboard/new` with no changes needed.
- Logout capability: Server Action (`src/app/logout/actions.ts`) that signs out via Supabase and redirects to `/login`.
- "Log Out" button added to the dashboard header, wired to the logout Server Action.
- Project details page (`src/app/(authenticated)/projects/[id]/page.tsx`) fetching a single project by id, showing client/project name, a status badge, and creation date, plus placeholder cards for the upcoming Client Requirements, 3PL List, Comparison, and Recommendation steps. Calls `notFound()` when no matching project exists. Uses the shared `(authenticated)` layout, so project rows on the dashboard now land here instead of 404ing.
- Client Requirements page (`src/app/(authenticated)/projects/[id]/requirements/page.tsx`) listing that project's `documents` rows grouped under RFI / Kickoff Meeting Transcript / Other Documents, with a fresh 60-second signed download URL generated per page load (never stored) and a "No documents uploaded yet" fallback per group; also includes a stub "RFI Template" section.
- Upload form (`upload-form.tsx`, Client Component) and Server Action (`src/app/(authenticated)/projects/[id]/requirements/actions.ts`) that uploads the chosen file to the `3pl-sourcing-documents` bucket under `${projectId}/${uuid}-${fileName}`, inserts the matching `documents` row, and revalidates the page so the new document appears immediately.
- The "Client Requirements" card on the project details page is now a working link to `/projects/[id]/requirements`; the other three placeholder cards are unchanged.
- Owner-only document delete: the requirements page now compares the current user's id to the project's `owner_id` and only renders a "Delete" control for the owner, opening a confirmation dialog (`delete-document-button.tsx`) before calling the new `deleteDocument` Server Action, which removes the storage object and `documents` row and relies entirely on RLS to reject non-owner deletes rather than duplicating the ownership check in application code.
- Requirements Summary form (`src/app/(authenticated)/projects/[id]/requirements/summary/page.tsx`), a Client Component form pre-filled from any existing `requirements_summary` row, covering Location, Storage Requirements, Order Volume, SKU Count, B2B or B2C, Special Handling Requirements, Target Cost, Required Turnaround Time, and Other Notes. The `saveRequirementsSummary` Server Action (`summary/actions.ts`) upserts the row by `project_id`, sets `updated_at`, and shows an inline "Saved" confirmation without redirecting. Linked from the Client Requirements page as "Requirements Summary", grouped with Step 1's document upload.
- 3PL List page (`src/app/(authenticated)/projects/[id]/providers/page.tsx`) listing that project's `providers` rows in a table (company, contact, location, status), with each status rendered as a pill using the exact per-status colors from `docs/DESIGN_SYSTEM.md`, per-cell Links for row navigation to `/projects/[id]/providers/[providerId]` (detail page not built yet — expect a 404 for now), an "Add Provider" button, and a design-system empty state.
- Add Provider form (`providers/new/page.tsx` + `new-provider-form.tsx`) and Server Action (`providers/new/actions.ts`) that inserts a row into `providers` with the project's id and the 7 exact status options, redirecting back to the 3PL list on success.
- The "3PL List" card on the project details page is now a working link to `/projects/[id]/providers`; Comparison and Recommendation are unchanged.
- Individual 3PL Details page (`src/app/(authenticated)/projects/[id]/providers/[providerId]/page.tsx`), replacing the 404 provider rows previously linked to: shows company name, status pill, and an info card (website, contact, email, phone, location, notes), plus that provider's `provider_documents` grouped under Discovery Call Transcript / Notes / Documents / Quotation / Revised Quotation, each with a fresh 60-second signed download URL. Upload form and `uploadProviderDocument` Server Action (`[providerId]/actions.ts`) store files under `providers/${providerId}/${uuid}-${fileName}` in the `3pl-sourcing-documents` bucket and revalidate the page on success.
- Extracted the provider status pill into a shared `status-badge.tsx` component, used by both the 3PL list and detail pages, to avoid the two copies drifting out of sync.
- Installed shadcn/ui (`npx shadcn@latest init`, Tailwind v4 CSS-variable mode, targeting the existing `src/styles/globals.css`) and added the `button`, `card`, `badge`, `table`, `sidebar`, and `dialog` components (plus their internal dependencies: `input`, `separator`, `skeleton`, `tooltip`, `sheet`, `use-mobile`) under `src/components/ui/` and `src/hooks/`. No pages were rebuilt to use them yet.

### Changed

- Applied the design system (`docs/DESIGN_SYSTEM.md`) across the login page, dashboard, new-project form, and logout button: Space Grotesk/Inter fonts wired up in the root layout via `next/font/google`, and colors, spacing, shape, and shadows brought in line with the documented tokens.
- Dashboard empty state now reads "No projects yet. Create your first one to get started." per the empty-state convention.
- Status pipeline (signature dotted element) intentionally deferred — no 3PL detail page exists yet to host it.
- Moved `src/app/dashboard/` into a `(authenticated)` route group (`src/app/(authenticated)/dashboard/`, with `new/` alongside it) so `/dashboard` and `/dashboard/new` URLs are unchanged.
- Added `src/app/(authenticated)/layout.tsx`, a shared Server Component header ("3PL Sourcing" + Log Out) rendered once above all authenticated pages instead of being duplicated per-page.
- Removed the now-redundant header row and Log Out button from the dashboard page; it keeps only its page-specific title and New Project button.
- Confirmed `src/middleware.ts` still protects `/dashboard` and `/dashboard/new`, since route groups don't change URL pathnames.
- Reapplied `docs/DESIGN_SYSTEM.md` across every page (login, shared header, dashboard, project details, client requirements + upload, requirements summary, 3PL list + add-provider, individual 3PL details) to match the Move brand subset: swapped Space Grotesk for Plus Jakarta Sans in the root layout, renamed every color token in `globals.css` (`ink-navy`→`move-navy`, `route-indigo`→`move-green`, `mist`→`neutral-bg`, `fog`→`neutral-border`, `slate`→`neutral-muted`, `danger-rose`→`danger`, etc.) and updated all usages, restyled the shared authenticated header to a solid Move Navy bar with white text and an outlined white Log Out button, and updated all 7 status pipeline badge colors. Token/color/font swap only — no layout, spacing, or behavior changes.
- Applied the new "Spacing & Layout Rhythm" section from `docs/DESIGN_SYSTEM.md` across all 9 authenticated pages: the header bar is now a fixed `h-16` with `px-8` and true vertical centering; every page container went from `px-4 py-8` to `px-8 py-10`; every page-title row now has `mb-8` before content starts; the dashboard and 3PL list tables now use `px-4 py-3` cells instead of `px-4 py-2`. Card padding audited and already met the `p-6` minimum everywhere, so no card changes were needed. Login's outer wrapper was left as-is since it isn't the `px-4 py-8` container pattern the rule targets.
- Replaced the horizontal top bar with a persistent sticky sidebar shell (`src/app/(authenticated)/layout.tsx` + new `sidebar-nav.tsx`): a `w-64` Move Navy sidebar with the wordmark up top, a "Dashboard" nav link with an active-state highlight (`sidebar-nav.tsx` is a Client Component using `usePathname` for that), and the authenticated user's email + Log Out button pinned to the bottom via `mt-auto`. Main content now renders in a `flex-1` Neutral Background area beside it; no other page needed changes since each page's own container is unaffected by the shell swap.
- Added a stats row to the dashboard (`dashboard/page.tsx`) — "Total Projects" and "Active Projects" counts, each its own `p-6` card — sitting between the title/"New Project" row and the projects table with `gap-6` spacing, using two lightweight `count`-only Supabase queries.
- Fixed the sidebar's "Dashboard" nav link not rendering: `sidebar-nav.tsx`'s `<nav>` had `flex-1` inside the sidebar's fixed `h-screen` flex column, which could get squeezed toward zero height under the wordmark and `mt-auto`-pinned bottom block; removed `flex-1` since `mt-auto` alone already pins the bottom section correctly. Also made the "3PL Sourcing" wordmark a `Link` to `/dashboard`.
- Capped every authenticated page's content at `max-w-5xl` (dropping the old `mx-auto` centering, left-aligned to match the sidebar) across all 8 pages — dashboard, project details, client requirements, requirements summary, 3PL list, provider details, and both "new" forms — with the two narrow forms keeping their own nested `max-w-sm` card so they don't stretch edge-to-edge.
- Added missing "← Back to ..." links audited against each page's logical parent: Project Details → Dashboard, New Project → Dashboard, Add Provider → 3PL List. The four pages that already had one (Client Requirements, Requirements Summary, 3PL List, Provider Details) were left unchanged.
- Fixed a sidebar/dashboard rendering pass: restored the "3PL Sourcing" wordmark's exact markup and gave the sidebar `pt-6` top spacing; fixed the dashboard stat cards' overlapping number/label by adding the `mt-2` that was missing between them; replaced the barely-visible active-nav outline with a solid filled state (`bg-[#2A3F73]` + `border-l-4 border-[#44B048]`); widened "New Project" to `px-5 py-2.5` with `whitespace-nowrap` so its text can't get clipped by the pill shape; and, after computing WCAG contrast ratios for every status badge, darkened the text color on the three that failed AA (Contacted 3.18:1, Quotation Received 2.73:1, Negotiation 3.24:1) to variants that pass at 5.4–6.2:1, documented inline in `status-badge.tsx`.
- Rewired shadcn's generated CSS variables in `src/styles/globals.css` to the Move brand palette instead of its neutral defaults: `--primary` is now Move Green (`#44B048`), `--sidebar`/`--sidebar-primary` (and their dark-mode equivalents) are Move Navy (`#192E5B`) with the same `#2A3F73` accent already used for the active sidebar nav item, `--destructive` maps to the existing `--color-danger` token, and `--background`/`--foreground`/`--muted`/`--border` map to the existing `neutral-bg`/`move-navy`/`neutral-muted`/`neutral-border` tokens so shadcn components inherit the same look as the rest of the app. Also removed an unrequested `Geist` font shadcn's init added to `layout.tsx`, and pointed the generated `--font-sans` at the existing Inter (`--font-body`) variable instead of leaving it self-referential. Fixed one pre-existing lint error (`react-hooks/set-state-in-effect`) in the generated `src/hooks/use-mobile.ts` by computing the initial value in `useState`'s initializer instead of setting it again inside the effect body.
- Rebuilt the sidebar, dashboard stat cards, status badges, and every hand-written button on top of the shadcn/ui components installed earlier: the sidebar (`layout.tsx` + `sidebar-nav.tsx`) now uses `Sidebar`/`SidebarHeader`/`SidebarContent`/`SidebarFooter`/`SidebarMenuButton`, which gives active/inactive nav contrast for free; the dashboard stat cards use `Card`/`CardHeader`/`CardContent` so the number and label stack structurally instead of relying on a hand-placed `mt-2`; every status pill (provider pipeline and the new shared `project-status-badge.tsx` for project status) now renders through `Badge`; and every submit button, link-styled CTA, and the delete-confirmation dialog's Cancel/Delete buttons now render through `Button` (link-styled CTAs use its polymorphic `render` prop with `nativeButton={false}`).
- While verifying this rebuild live in a browser (previous verification passes only grepped compiled CSS, never actually measured rendered layout), found and fixed a real, long-standing bug: the global `* { padding: 0; margin: 0; }` reset in `globals.css` was un-layered CSS, and per the CSS cascade-layers spec un-layered rules always beat rules inside any `@layer` — including Tailwind's own `utilities` layer. That silently zeroed out every `px-*`/`py-*` utility across the entire app. Fixed by wrapping the reset (and the other plain `html`/`body`/`a` rules) in `@layer base`.
- Fixed the sidebar not stretching to full viewport height on pages with little content: `Sidebar`'s `collapsible="none"` path (used for our always-visible sidebar) sizes itself with `h-full`, which only resolves against a containing block with an explicit (non-`min-`) `height` — ours only had `min-h-svh`, so the browser fell back to the sidebar's content height instead of stretching. Fixed by giving the `Sidebar` in `layout.tsx` `className="sticky top-0 h-svh ..."` — `h-svh` sizes directly off the viewport instead of depending on an ancestor's resolved height, and `sticky top-0` keeps it pinned full-height while the main content scrolls on pages taller than the viewport.
- Reworked `(authenticated)/layout.tsx` into a fixed-height app shell instead of relying on document-level scrolling with a `sticky` sidebar: `SidebarProvider` is now `h-svh overflow-hidden` (was `min-h-svh`), `Sidebar` dropped the `sticky top-0 h-svh` override (plain `h-full` now correctly resolves against the shell's explicit height instead of falling back to content height), and `SidebarInset` gained `overflow-y-auto` so it becomes its own internal scroll container. The sidebar itself now never needs to scroll — it simply doesn't have to, since it's a fixed participant in a fixed-height row — which is more robust than depending on `position: sticky` tracking document scroll. Verified the Requirements Summary form (tall enough to overflow a typical viewport) scrolls to reveal the Save button while the sidebar stays completely static, and checked the Dashboard and 3PL List pages behave the same.
- Added data-type constraints to the fields identified in a full form-field audit as genuinely typed rather than free text: `requirements_summary.sku_count` is now `type="number" min="0" step="1"` (was `text`); `providers.phone` is now `type="tel"` (was `text`, no pattern — international formats vary too much to validate strictly); `providers.website` stays `type="text"` but `providers/new/actions.ts` now prepends `https://` server-side when the submitted value doesn't already start with `http://`/`https://`, so bare domains like `example.com` save as valid URLs; and both document upload forms (`requirements/upload-form.tsx`, `providers/[providerId]/upload-form.tsx`) gained `accept="application/pdf,.doc,.docx,image/png,image/jpeg"` as a file-picker hint only — nothing is blocked server-side. Fields with units, ranges, or currency (`order_volume`, `target_cost`, `turnaround_time`) were confirmed to correctly stay free text.
- Added Provider editing: `providers/[providerId]/edit/` (`page.tsx`, `edit-provider-form.tsx`, `actions.ts`) pre-fills a form from the existing row and `updateProvider` runs an `.update().eq("id", ...)` against it (no new RLS policy needed — the existing "Authenticated users can update providers" policy already covers it), redirecting back to the provider's detail page on success. The add- and edit-provider forms were unified into one shared `providers/provider-form.tsx` component so both stay in sync going forward. An "Edit" button was added to the Individual 3PL Details page header.
- Added live (pre-submit) field validation to the add/edit-provider forms: `company_name` shows "Company name is required" on blur if empty, and `email` shows "Enter a valid email" on blur if the value doesn't match a basic email pattern — both render immediately as inline red helper text, not only after clicking Save.
- Added placeholder example text (`placeholder:italic placeholder:text-gray-400`) to the fields listed in the field-type audit that stay free text: all of `requirements_summary`'s free-text fields, `providers.company_name`/`website`/`contact_person`/`email`/`location`, and the project-creation form's `client_name`/`project_name`.
- Replaced the single `providers.phone` input with a country-code `<select>` (`+1`/`+44`/`+61`/`+63`/`+65`/`+91`/`+971`, defaulting to `+1`) plus a digits-only text input (non-digit characters are stripped on every keystroke via `onChange`, not `type="number"`, to avoid spinner arrows and leading-zero mangling) on both the add- and edit-provider forms. The two are combined into a single string (e.g. `"+63 9171234567"`) server-side before writing to the existing `phone` column — no schema change. Editing an existing provider splits its stored `phone` value back into a country code and digits by matching against the known list of codes.
- Investigated a reported file-upload regression on the document upload forms; could not reproduce it on the current code — both forms were tested end-to-end (button `type="submit"` intact after the shadcn `Button` swap, correctly form-associated, POST returns 200, uploaded file appears with a working download link, zero console errors). No fix applied since no defect was found.
- Replaced the native `<select>` for provider status (add- and edit-provider forms) with shadcn's `Select` (newly installed via `npx shadcn@latest add select`), styled to match the rest of the form (`rounded-xl`, `border-neutral-border`, full width) instead of rendering as an oversized unstyled native dropdown. Each option now shows a small colored dot matching its status color.
- Redesigned the provider status color scheme so only "Rejected" reads as negative/red — every other status now reads progressively more positive toward Move Green as the pipeline nears "Vetted" (`Potential` neutral gray → `Contacted` light blue → `Discovery Call` teal → `Quotation Received` light green → `Negotiation` stronger green → `Vetted` Move Green family). Updated in `docs/DESIGN_SYSTEM.md`'s status pipeline table, the shared `STATUS_STYLES` badge map, and the new status Select's dot indicators, all from one source (`status-badge.tsx`) so they can't drift out of sync.
- Added provider deletion: a destructive "Delete Provider" button on the Individual 3PL Details page opens a shadcn `Dialog` confirmation ("Delete [company_name]? This will also delete all of its uploaded documents. This cannot be undone.") rather than a hand-rolled portal modal. The `deleteProvider` Server Action (`[providerId]/actions.ts`) fetches the provider's `provider_documents` rows, removes each corresponding file from the `3pl-sourcing-documents` bucket, deletes the `provider_documents` rows, then deletes the `providers` row, redirecting to the project's 3PL list on success — relying entirely on RLS, no app-level permission checks. Verification surfaced a genuine pre-existing storage RLS policy bug (a `::uuid` cast on the first path segment that broke on provider-document paths' `providers/` prefix); after that policy was fixed at the database level, confirmed end-to-end that deleting a provider removes it from the list and its storage file returns 404/`NoSuchKey`, not just its database row.
